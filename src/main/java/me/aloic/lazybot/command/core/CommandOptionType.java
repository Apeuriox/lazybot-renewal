package me.aloic.lazybot.command.core;

/** Platform-neutral option types supported by structured command adapters. */
public enum CommandOptionType {
    STRING,
    INTEGER,
    BOOLEAN,
    NUMBER
}
