<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.aloic.lazybot.osu.dao.mapper.TokenMapper">
    <resultMap id="BindingResultMap"
               type="me.aloic.lazybot.osu.dao.entity.po.AccessTokenPO">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="platform_identity_id" jdbcType="BIGINT" property="platform_identity_id"/>
        <result column="platform" jdbcType="VARCHAR" property="platform"/>
        <result column="platform_user_id" jdbcType="VARCHAR" property="platform_user_id"/>
        <result column="osu_account_id" jdbcType="BIGINT" property="osu_account_id"/>
        <result column="player_id" jdbcType="INTEGER" property="player_id"/>
        <result column="player_name" jdbcType="VARCHAR" property="player_name"/>
        <result column="server" jdbcType="VARCHAR" property="server"/>
        <result column="link_method" jdbcType="VARCHAR" property="link_method"/>
        <result column="verified_at" jdbcType="TIMESTAMP" property="verified_at"/>
        <result column="default_mode" jdbcType="VARCHAR" property="default_mode"/>
        <result column="default_subset" jdbcType="VARCHAR" property="default_subset"/>
        <result column="preferred_panel_version" jdbcType="INTEGER"
                property="preferred_panel_version"/>
        <result column="enabled" jdbcType="BOOLEAN" property="enabled"/>
        <result column="avatar_etag" jdbcType="VARCHAR" property="avatar_etag"/>
        <result column="avatar_last_checked" jdbcType="TIMESTAMP"
                property="avatar_last_checked"/>
        <result column="avatar_next_check_at" jdbcType="TIMESTAMP"
                property="avatar_next_check_at"/>
    </resultMap>

    <sql id="BindingSelect">
        select u.id,
               p.id as platform_identity_id,
               p.platform,
               p.platform_user_id,
               a.id as osu_account_id,
               a.osu_user_id as player_id,
               a.username_cache as player_name,
               a.server,
               a.link_method,
               a.verified_at,
               u.default_mode,
               u.default_subset,
               u.preferred_panel_version,
               u.enabled,
               a.avatar_etag,
               a.avatar_last_checked,
               a.avatar_next_check_at
        from lazybot_user u
        join platform_identity p on p.lazybot_user_id = u.id
        join osu_account a on a.lazybot_user_id = u.id
    </sql>

    <select id="selectByQq_code" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where p.platform = 'qq'
          and p.platform_user_id = cast(#{qq_code} as char)
          and a.server = 'bancho'
        limit 1
    </select>

    <select id="selectRandom" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where p.platform = 'qq'
          and a.server = 'bancho'
          and u.enabled = true
        order by rand()
        limit 1
    </select>

    <select id="selectById" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where u.id = #{id}
          and a.server = 'bancho'
        limit 1
    </select>

    <select id="selectByPlayername" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where a.server = 'bancho'
          and a.username_cache = #{player_name}
        limit 1
    </select>

    <select id="selectByPlayerId" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where a.server = 'bancho'
          and a.osu_user_id = #{player_id}
        limit 1
    </select>

    <select id="selectByCodes" resultMap="BindingResultMap">
        <include refid="BindingSelect"/>
        where p.platform = 'qq'
          and a.server = 'bancho'
          and u.enabled = true
          and p.platform_user_id in
        <foreach collection="codes" item="code" open="(" separator="," close=")">
            cast(#{code} as char)
        </foreach>
    </select>
</mapper>
