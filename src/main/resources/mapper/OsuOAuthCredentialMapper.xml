<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.aloic.lazybot.osu.dao.mapper.OsuOAuthCredentialMapper">
    <resultMap id="OsuOAuthCredentialResultMap"
               type="me.aloic.lazybot.osu.dao.entity.po.OsuOAuthCredentialPO">
        <id column="osu_account_id" jdbcType="BIGINT" property="osu_account_id"/>
        <result column="access_token" jdbcType="VARCHAR" property="access_token"/>
        <result column="refresh_token" jdbcType="VARCHAR" property="refresh_token"/>
        <result column="access_token_expires_at" jdbcType="TIMESTAMP"
                property="access_token_expires_at"/>
        <result column="granted_scopes" jdbcType="VARCHAR" property="granted_scopes"/>
        <result column="row_version" jdbcType="BIGINT" property="row_version"/>
        <result column="created_at" jdbcType="TIMESTAMP" property="created_at"/>
        <result column="updated_at" jdbcType="TIMESTAMP" property="updated_at"/>
    </resultMap>

    <sql id="OsuOAuthCredentialColumns">
        osu_account_id, access_token, refresh_token, access_token_expires_at,
        granted_scopes, row_version, created_at, updated_at
    </sql>

    <select id="selectByAccountIdForUpdate"
            resultMap="OsuOAuthCredentialResultMap">
        select <include refid="OsuOAuthCredentialColumns"/>
        from osu_oauth_credential
        where osu_account_id = #{accountId}
        for update
    </select>

    <update id="updateRotatedCredential">
        update osu_oauth_credential
        set access_token = #{credential.access_token},
            refresh_token = #{credential.refresh_token},
            access_token_expires_at = #{credential.access_token_expires_at},
            granted_scopes = #{credential.granted_scopes},
            row_version = row_version + 1,
            updated_at = #{credential.updated_at}
        where osu_account_id = #{credential.osu_account_id}
          and row_version = #{expectedVersion}
    </update>
</mapper>
